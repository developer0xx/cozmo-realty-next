/* import io from 'socket.io-client';

const socket = io('http://localhost:5000', {
	withCredentials: true,
  }); // open connection to backend side.

const newLeadNotice = cb => {
	socket.on('newLead', leadInfo => cb(leadInfo));
}

export { newLeadNotice }; */